
/* bind events to buttons
$("input[name='treecover2000']")
  .on('click', function() {
    if(webmap.hasLayer("treecover2000")) {
      $(this).removeClass('checked');
      webmap.removeLayer("treecover2000");
  } else {
    webmap.addLayer("treecover2000");        
      $(this).addClass('checked');
 }
  })
;*/
