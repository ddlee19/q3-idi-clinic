/**
 * Represents a consumer brand record that filters the current view on click.
 */
export interface BrandFilter {
    country: string;
    id: number;
    name: string;
    rspo_member_since: string;
}